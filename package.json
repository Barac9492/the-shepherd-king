{
  "name": "the-shepherd-king",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build:assets": "node scripts/build-storybook-assets.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node scripts/browser-check.mjs"
  },
  "devDependencies": {
    "three": "0.160.0",
    "@playwright/test": "1.58.2"
  }
}
